# asteroid-game

Asteroids clone built with `pygame`. Ships, shots and asteroids all inherit from a
single `CircleShape` base class that handles position, velocity and collision.
Large asteroids split into smaller ones when hit.

```bash
uv sync
uv run main.py
```

Object-oriented programming project from the [Boot.dev](https://www.boot.dev) course.
